Managers are orchestrators or aggregators. This is needed
when you're creating a class that does multiple logic.
For example, you have an operation, that gets, and saves records but also do calculation and then sending information to a 3rd party
endpoints. This is usable for those kind of usecases.

This is a wrapper service class that is expected to use Persistence services and BusinessLogic services and Microservices